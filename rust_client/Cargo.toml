[package]
name = "findex-client"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
base64 = "0.22"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
criterion = "0.7.0"

[[bench]]
name = "findex_client"
harness = false

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
